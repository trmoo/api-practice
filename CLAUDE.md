# api-practice — API 실습실 (개발 기록)

## 앱의 목적과 대상 단원

주소를 부르면 자료가 온다는 것을 직접 조작해 보고, **나이스 급식 API 로 우리 학교 자료를
수집해 보는** 수업용 웹앱. 고등학교 「정보」·「데이터 과학」 **1차시**용.

성취기준 두 개 모두 핵심 동사가 **「수집」**이라 이 앱이 곧 성취기준이 된다.

- `[12정02-03]` 빅데이터의 개념과 특징에 대한 이해를 바탕으로, 문제 해결에 적합한 데이터를 수집한다.
  - 해설에 「공개된 공공 데이터나 민간 데이터 활용 등 다양한 데이터 수집 방법을 경험」이 명시되어 있다.
- `[12데과02-01]` 데이터를 편향되지 않도록 수집하고, 수집된 데이터의 특성을 분석한다.
  - 해설에 「공공 데이터 포털, 출처가 명확한 민간 데이터 포털을 통해」가 있다.

## 기능 목록

탭 2개 · 화면 9개 · 채점 활동 11가지(만점 11).
주소 해부기에 **Type=json ↔ Type=xml 형식 실험**이 붙어 있다(채점 안 함).

**① 요청과 응답** — 첫 요청(강아지·날씨·위키 중 골라 보내기, 4단계 애니메이션) /
**값 하나만 바꾸기**(에듀넷 `clss_id` 일곱 값 + 퀴즈 2 + CORS 실험) /
주소 해부기(색으로 조각내기 + 퀴즈 3, 인코딩 바이트 표는 접이식) /
파라미터 실험실(도시·항목·시간대를 고르면 주소 실시간 조립) /
JSON 탐험기(나무에서 값을 누르면 경로가 JS·파이썬 두 문법으로).

**② 급식 API 실습** — 학교 찾기(검색 → 코드 얻기 + 코드 빈칸 1) /
급식 조회(진짜 급식 + 날짜 실험 4가지) / 경로 찾기(나무를 눌러 답하는 문제 3) /
전처리·마무리(`<br/>` 자르기 · 알레르기 번호 풀이 · 퀴즈 3 · 점수판).

## 현재 개발 상태

**2026-08-30 — 1차시 9화면 전부 완성.** 시험 314가지 · 문법 검사 통과 · 빌드 확인.
실제 호출과 연습 모드 양쪽을 브라우저에서 끝까지 눌러 확인했다.

### 다음 할 일 (사용자 요청 범위 밖이라 미뤄 둔 것)

원래 계획한 14화면 중 **6화면을 다음 차시로 미뤘다.**

- **③ 함정** — CORS 실험(되는 API / 막히는 API 나란히) / 키 노출(`data.go.kr` 주소에 키가 보인다,
  왜 서버를 거치는가) / 오류 처리 4가지를 직접 만들어 보기
- **④ 코드로** — 화면에서 만든 요청을 fetch·requests 코드로 조립 / 여러 날 모아 칼로리 꺾은선 그래프 /
  배움 지도(무엇이 진짜 호출이고 무엇이 연습 데이터인지 표로)

---

## ★ 이 앱만의 근본 구조 — 이중 모드

**다른 앱과 달리 이 앱은 본질상 진짜 인터넷 요청을 한다.** 교실 인터넷이 끊기거나
학교 방화벽이 막으면 수업이 통째로 멈춘다. 그래서 `src/lib/api.js` 에 모드를 둘 두었다.

| 모드 | 동작 |
|---|---|
| 🌐 실제 호출 | 진짜 `fetch`. 상태 코드·걸린 시간을 그대로 보여 준다 |
| 📦 연습 모드 | `src/data/samples.js` 에 담아 둔 **진짜 응답**으로 똑같이 동작 |

- 요청이 실패하면 앱이 스스로 연습 모드를 권한다(`연습모드권함()`).
- **`npm test` 는 연습 모드로 돈다.** 그래서 인터넷 없이 검증된다.
- ⚠ **연습 모드가 실제와 다르게 동작하면 학생이 틀린 것을 배운다.**
  그래서 5건 제한·INFO-200·GMT 환산까지 그대로 재현하고, `test/api.test.mjs` 가 그것을 지킨다.
- 두 모드가 실제로 다른 점(연습 모드에는 고등학교 140곳만, 실제는 초·중·고 전부)은
  **감추지 않고 화면에 밝힌다.**

---

## 에듀넷 API — 파라미터 도입부 (2026-08-30 추가)

「내용이 어렵다」는 사용자 지적에 따라 **가장 쉬운 화면을 2번 자리에 넣었다**.
근거 자료는 「에듀넷 주제별 학습자료 API(v1.3) 사용 매뉴얼」(KERIS, 2025-06, `.hwp`).

**왜 이 API 가 파라미터 수업에 좋은가** — 요청 변수가 `clss_id` **하나뿐**이고
값이 일곱 개로 정해져 있다. 숫자 하나만 바꾸면 결과가 통째로 바뀌어 인과가 선명하다.

| clss_id | 과목 | 전체 |
|---|---|---|
| 362 | 사회 | 590건 |
| 363 | 과학 | 468건 |
| 75541 · 75542 · 75543 · 75544 | 음악 · 미술 · 체육 · 기술가정실과 | 87~94건 |
| 89162 | 범교과 학습 주제 | 250건 |

⚠⚠ **이 API 는 브라우저에서 직접 못 부른다** (2026-08-30 확인)
- **CORS 헤더가 없다** — 일곱 주소 모두 `Access-Control-Allow-Origin` 을 안 보낸다.
- **파일이 크다** — 1.1MB(체육) ~ 6.0MB(사회). `<expln>` 한 건이 1만 자 HTML이다.

그래서 `tools/fetch_edunet.mjs` 로 한 번 받아 **제목·키워드·링크만** 남겨
`src/data/edunet.js`(24KB)에 담았다. 화면은 그것으로 돌고, **[진짜로 불러 보기] 단추**로
막히는 것을 학생이 직접 보게 한 뒤 CORS 를 가르친다. 한계를 수업 소재로 바꾼 것이다.

⚠ **저작권 — 본문(`expln`)을 담으면 안 된다.** 남의 콘텐츠를 재배포하는 셈이 된다.
`npm test` 가 `expln` 이 섞여 들어왔는지 검사한다. 출처는 KERIS 에듀넷.

⚠ `api.부르기()` 는 spec 에 `연습전용: true` 가 있으면 **실제 모드에서도** 담아 둔 자료를 쓴다.
그 사실을 화면에 「📦 담아 둔 자료」로 밝힌다. 감추면 안 된다.

## 쉽게 고친 것 (2026-08-30 사용자 요청)

- **한글 인코딩 바이트 표** → 접이식 `details` 로 내렸다(기본 닫힘). 본문에는 결론만 남겼다.
- **경로 찾기 5문항 → 3문항** (건수·처리코드 문항을 뺐다).
- **코드 빈칸 채우기 → 읽기 자료로.** `req.js` 의 `코드읽기()` 가 정답을 채운 코드와
  설명을 보여 주고 **채점하지 않는다**. 문법을 외우는 데 시간을 쓰지 않게 하려는 것.
  그래서 채점 활동이 15 → 11 가지가 되었다.

## ⚠⚠ 반드시 기억할 것

### `fetch` 에 헤더를 함부로 붙이지 말 것 (2026-08-30 실제로 겪음)

`Accept: application/json` 을 붙였더니 **나이스가 `Access-Control-Allow-Origin` 헤더를
아예 안 보내** 브라우저가 요청을 막았다. `curl` 로는 멀쩡히 되고 브라우저에서만 죽어
원인을 찾기 어려웠다. 확인한 결과:

| 보낸 것 | ACAO 헤더 |
|---|---|
| Origin 없음 / 어떤 Origin이든 | `*` (온다) |
| **`Accept: application/json` 을 붙이면** | **안 옴** |

`Type=json` 파라미터로 이미 형식을 정하므로 헤더는 필요 없다. **붙이지 말 것.**

### ⚠ JSON 만 오는 것이 아니다 — 나이스는 Type 을 빼면 기본이 XML (2026-08-30)

「JSON이 아니라 XML이 올 수도 있지 않냐」는 사용자 지적에서 확인한 것.

| 보낸 것 | Content-Type | 돌아오는 것 |
|---|---|---|
| `Type=json` | `application/json` | JSON |
| `Type=xml` | `application/xml` | XML |
| **`Type` 를 아예 빼면** | `application/xml` | **XML** ← 기본값이 XML 이다 |

셋 다 CORS `*` 라 브라우저에서 실제로 다 된다.
학생이 주소 해부기에서 `Type=json` 을 지우면 XML 이 와서 `JSON.parse` 가 실패한다.
그래서 `부르기()` 가 Content-Type 을 보고 **「XML 로 왔습니다」라고 따로 안내**하고,
`날글` 옵션으로 원문을 그대로 볼 수 있게 했다.

주소 해부기에 **형식 실험 카드**를 넣었다 — `Type=json` ↔ `Type=xml` 을 눌러
같은 자료가 두 모양으로 오는 것을 나란히 본다. 파라미터 하나가 응답의 「모양 자체」를
바꾸는 예라 파라미터 수업으로도 좋다.

연습 모드에서는 `나이스XML()` 이 담아 둔 JSON 을 XML 로 옮겨 적어 보여 준다.
⚠ 들여쓰기까지 실제와 같지는 않다. **구조만** 같게 맞췄고 그 사실을 주석에 적어 두었다.

⚠ 그래서 앱 제목을 「주소를 부르면 **JSON이** 온다」 → 「주소를 부르면 **자료가** 온다」로
바꿨다. 2번 화면(에듀넷)이 XML 로만 답하므로 예전 제목은 사실과 어긋났다.

### 나이스는 인증키 없이 쓰면 언제나 첫 5건만 준다

- `pSize` 를 1000 으로 키워도 5건.
- **`pIndex` 로 쪽을 넘겨도 무시된다** — 언제나 같은 첫 5건이 온다.
- 그래서 전국 목록을 통째로 받을 수 없고, `tools/fetch_samples.mjs` 가
  시도교육청 17곳 + 검색어 30개를 돌려 140곳을 모았다.
- **다행히 이름 부분 검색이 된다** (`와우` → 와우고등학교). 실제 수업에서는 이걸로 충분하다.
- 이 제한 자체가 좋은 수업 소재라 화면에 그대로 가르친다(인증키의 존재 이유).

### HTTP 200 인데 자료가 없는 경우

나이스는 자료가 없어도 **200** 을 주고 본문 모양을 통째로 바꾼다.

```
자료 있음 → { "mealServiceDietInfo": [ {head:[…]}, {row:[…]} ] }
자료 없음 → { "RESULT": { "CODE": "INFO-200", "MESSAGE": "해당하는 데이터가 없습니다." } }
필수값 누락 → { "RESULT": { "CODE": "ERROR-300", … } }
```

`api.나이스풀이()` 가 이것을 갈라 준다. **이 앱에서 가장 중요한 가르침이다.**

### 배포 워크플로의 셸 변수 이름은 반드시 ASCII 로 (2026-08-30 실제로 겪음)

`크기=$(wc -c < dist/index.html)` 처럼 한글 변수 이름을 쓰면
**bash 가 `command not found` 로 죽는다.** 이 앱의 다른 곳(자바스크립트·파이썬)에서는
한글 이름을 쓰지만 **셸 스크립트만은 예외**다. `size` · `count` 처럼 영문으로 쓸 것.
첫 푸시 전에 잡았지만, 안 잡았으면 배포가 통째로 깨졌을 것이다.

### 브라우저 도구로는 `file://` 을 못 연다

`dist/index.html` 을 더블클릭해 여는 것은 **손으로 확인해야 한다.**
대신 확인해 둔 것 —
① 번들에 바깥 자원 참조가 **0개**다(`<script>` 1개 · `<style>` 1개, 나머지는 전부 인라인).
② 부르는 API 다섯 곳 모두 `Origin: null` 에도 `Access-Control-Allow-Origin: *` 를 준다
(`file://` 에서 보내는 Origin 이 `null` 이다). 그래서 더블클릭으로도 실제 호출이 될 것이다.
③ `sessionStorage` 가 막혀도 `store.js` 가 try/catch 로 넘어간다.

### 파워셸 `Get-Content` 로 빌드 결과를 확인하지 말 것

BOM 없는 UTF-8 을 CP949 로 읽어 한글이 깨진다. `grep` 이나 파이썬(`PYTHONIOENCODING=utf-8`)으로 확인한다.

### 빌드 전에 `npm run check:syntax` 를 반드시 함께 돌릴 것

`npm test` 는 화면을 그리는 탭 파일(`src/tabs/*.js`)을 부르지 않는다.
탭 파일에 괄호를 하나 빠뜨려도 시험은 통과하고 빌드에서야 터진다.
`tools/check_syntax.mjs` 가 esbuild 로 전수 문법 검사 + 저작권 머리 주석 + 화면 수명 규칙을 본다.

⚠ 이 검사는 **그냥 `import` 로는 못 한다** — `main.js` 가 `./style.css` 를 부르는데
그것은 Vite 만 아는 문법이라 node 가 「Unknown file extension」으로 죽는다.
그래서 문법은 esbuild `transformSync` 로 훑고, 진짜 불러오기는 CSS 를 안 쓰는 파일에만 한다.

### 화면 수명 — 탭 파일에서 `window.addEventListener`·`setInterval` 을 직접 쓰지 말 것

`src/lib/ui.js` 의 `beginScreen()`·`onWindow()`·`screenTimeout()` 을 쓴다.
`check_syntax.mjs` 가 어긴 것을 잡는다. 브라우저 기본 `alert`·`confirm`·`prompt` 도 막는다
(크롬이 「…에 삽입된 페이지 내용:」 이라는 주소를 붙여 학생에게 보인다).

### 단추의 수식 클래스를 홀로 쓰는 클래스와 같은 이름으로 두지 말 것

`.big` 은 통계 숫자용(초록 글씨)이라 `.btn.big` 을 쓰면 색이 새어
초록 배경에 초록 글씨가 된다. 그래서 **`.btn.lg`** 를 쓴다.

### 알약·탭 줄 가운데 맞추기는 `margin:auto` 로

`justify-content:center` 를 쓰면 줄이 넘칠 때 왼쪽 끝이 잘려 스크롤로도 닿지 못한다.
첫·마지막 항목에 `margin-left/right:auto` 를 준다.

### 채점이 붙는 고르기는 처음에 아무것도 골라 두지 않는다

첫 항목이 기본 선택이면 학생이 안 고르고 [확인] 을 눌러도 골라진 것으로 처리되어
채점이 무의미해진다. `req.js` 의 `고르기문제()`·`코드빈칸문제()` 가 그렇게 되어 있다.

### 문제를 늘릴 때는 세 곳을 함께 고칠 것

`src/data/quiz.js` 의 문제 배열 · 같은 파일의 `활동목록` · 화면에서 부르는 곳.
`npm test` 가 「활동 수 = 문제 수 합계」와 「열쇠가 겹치지 않는가」를 대조한다.

### `src/data/*.js` 와 `src/lib/*.js` 에서 최상위에 DOM 을 만들지 말 것

node 로 그대로 불러다 시험한다. `json.js` 는 `ui.js` 를 부르지만 둘 다
함수 안에서만 `document` 를 건드리므로 node 에서 불러진다.

---

## 검증된 수치 (고칠 때 함께 갱신할 것)

| 값 | 얼마 | 어디서 확인 |
|---|---|---|
| 시험 | **329가지** | `npm test` |
| 화면 | 9개 (탭 2개) | `src/main.js` 의 `탭들` |
| 채점 활동 | 11가지 (코드 빈칸은 채점 안 함) | `quiz.js` 의 `활동목록` |
| 연습 모드 학교 | 140곳 | `samples.js` |
| 연습 모드 급식 | 10개교 × 5일치 (2026-08-24~28) | `samples.js` |
| 나이스 무키 최대 건수 | **5** | `samples.js` 의 `무키최대건수` |
| 전국 고등학교 | 2,409개교 | 2026-08-30 나이스 `list_total_count` |
| 알레르기 표 | 19종 | `quiz.js` 의 `알레르기표` |
| `dist/index.html` | 약 185KB | `npm run build` |
| `grep -c 티쳐무 dist/index.html` | **16** | 소스 14개 머리 주석 + HTML 주석 + 배너 (푸터는 같은 줄) |

⚠ **저작권 표시 횟수가 다른 앱(3)과 다르다.** 소스 파일마다 `/*!` 머리 주석이
`esbuild.legalComments:'inline'` 때문에 그대로 남기 때문이다(`saints_dict` 가 30인 것과 같은 이유).
소스 파일을 늘리면 이 수도 는다. **배포 워크플로의 검사 기준을 함께 고칠 것.**

## 저작권 표시를 넣은 곳

`LICENSE` · `README.md` · `CLAUDE.md`(이 파일) · `package.json`(`UNLICENSED`) ·
`index.html` 주석 · `src/` 14개 파일 머리 주석 · **화면 푸터**(`ui.js` 의 `footer()`) ·
`vite.config.js` 의 `rollupOptions.output.banner` · `tools/` 3개 · `test/` 4개.

⚠ **`LICENSE` 에 「이 앱이 부르는 API 에 관하여」 절이 따로 있다.**
연습 모드에 담아 둔 응답은 남의 자료다 — 특히 **위키백과 요약문은 CC BY-SA 4.0** 이라
`LICENSE` 와 **앱 화면 양쪽에** 출처를 밝혔다. 지우지 말 것.

## 원본 자료

없다. 이 앱은 교안·학습지에서 온 것이 아니라 「API 사용법 수업」이라는 요청에서 시작했다.
쓰이는 자료는 모두 공개 API 에서 그때그때 받아 온다.

`npm run data` 로 연습 모드용 응답을 다시 받는다. 받는 날짜가 바뀌면
`samples.js` 의 `받은날` 과 급식 날짜가 함께 바뀌므로 `npm test` 를 다시 돌려야 한다
(급식 날짜를 박아 둔 곳은 없지만, 연습 모드 급식이 있는 학교 수가 달라질 수 있다).

## 학습지 (2026-08-30 추가)

`docs/` 에 워드 문서 둘. **우리가 직접 만든 것이라 저장소에 넣는다**(남의 교안이 아니다).
`.gitignore` 가 `*.docx` 를 막되 `!docs/*.docx` 로 이 둘만 통과시킨다.

| 파일 | 무엇 |
|---|---|
| `API실습_학습지.docx` | 학생용 9쪽 — 활동 8개 + 마무리. 활동 번호가 앱 화면과 1:1 |
| `API실습_교사용.docx` | 교사용 9쪽 — 정답 · 시간 배분(50분) · 평가 요소 · 문제 상황 대처 |

⚠ **교사용에는 정답이 있으니 학생 배부 금지.** 머리글에 그렇게 적어 두었다.

다시 만들려면 스크래치패드의 `docxgen/`(`common.js`·`student.js`·`teacher.js`)이 필요하다 —
`npm i docx` 뒤 `node student.js <나갈길>`. **앱 폴더에 `docx` 를 설치하지 말 것**
(프로젝트 규칙: 앱 의존성을 늘리지 않는다). 확인은 LibreOffice 로 PDF 변환 뒤
PyMuPDF 로 PNG 를 뽑아 눈으로 본다(이 PC 에 `pdftoppm` 이 없다).

⚠ 문제·정답을 고치면 **`src/data/quiz.js` 와 학습지 두 문서를 함께** 고칠 것.

## 저장소·배포

저장소: https://github.com/trmoo/api-practice (2026-08-30 첫 푸시)

### ⚠⚠ Pages 「Deploy from a branch」 함정 — 실제로 걸렸다 (2026-08-30, 해결)

첫 푸시 때 Pages Source 가 「Deploy from a branch」 로 되어 있어 **흰 화면**이 나왔다.
`saints_dict` 에서 겪은 것과 같은 함정에 다시 걸린 것이다.
사용자가 Source 를 「GitHub Actions」 로 바꾼 뒤 다시 푸시해 해결했다.

**★ 이때 알게 된 것 — 설정을 바꾸는 것만으로는 화면이 안 바뀐다.**
바꾼 뒤 **새로 한 번 배포해야** 한다. 설정 변경 전의 마지막 배포는 Jekyll 것이라
그대로 남아 있기 때문이다. 실제로 우리 잡은 10:32:47 에, Jekyll 은 10:32:53 에 끝나
**6초 차이로 Jekyll 이 이겼다.**

증상과 확인 방법 —

```
curl -s https://trmoo.github.io/api-practice/ | wc -c
```

- **1,311바이트** 가 나오면 잘못된 것이다. 저장소 루트의 개발용 `index.html`(껍데기)이
  그대로 서비스되는 중이며, 그 안의 `/src/main.js` 는 404 라 **흰 화면**이 된다.
- **186,033바이트**(약 181KB) 가 나와야 정상이다.

까닭 — Pages Source 가 「Deploy from a branch」 라서 깃허브가 붙이는
`pages build and deployment`(Jekyll) 잡이 푸시마다 함께 돌고, **나중에 끝난 쪽이 이긴다.**
우리는 `dist/` 를 커밋하지 않으므로 브랜치 배포는 원리상 동작할 수 없다.

⚠ **Actions 탭만 보면 모른다.** 우리 워크플로도 성공, Jekyll 도 성공으로 찍힌다.
실제 크기를 재야 안다.

고치는 법 — **Settings → Pages → Source 를 「GitHub Actions」 로 바꾼 뒤 Re-run.**
API 로는 `PUT /repos/trmoo/api-practice/pages` 에 `{"build_type":"workflow"}`
(`legacy` 면 브랜치, `workflow` 면 Actions).

⚠ `configure-pages@v5` 의 `enablement: true` 는 **꺼진 Pages 를 켜 줄 뿐,
이미 켜진 브랜치 배포를 Actions 배포로 바꾸지 못한다.**

⚠ REST API 의 `/pages` 엔드포인트는 인증이 필요해 익명으로는 404 가 나온다.
켜졌는지 여부를 이것으로 판단하지 말 것 — `curl -sI` 로 200 인지, `wc -c` 로 크기를 볼 것.

⚠ 깃 전역 `user.name` 이 비어 있어(전역 이메일은 `enssam21@gmail.com`) 폴더에 따로 넣었다 —
`user.name trmoo` · `user.email trmoo@users.noreply.github.com`.

Pages 를 켤 때는 Settings → Pages → Source 를 **「GitHub Actions」** 로 할 것.
(「Deploy from a branch」를 고르면 빌드 안 한 껍데기가 서비스되어 흰 화면이 된다 —
`saints_dict` 에서 실제로 겪은 함정이다.)
